sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t TianranWei/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true TianranWei/docker-react npm run test
